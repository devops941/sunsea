/**
 * Theme toggle removed — app is dark-mode only.
 * Component kept as a no-op so existing imports don't break.
 */
const ThemeToggle: React.FC<{ className?: string }> = () => null;

export default ThemeToggle;
